// jshint esversion: 6

/*
    设备：mi-max2
*/
const dpi = 2.75    // Dots Per Inch
const hpx = 1920    // height in pixels
const wpx = 1080    // width in pixels
const hdp = hpx/dpi    // height in dp
const wdp = wpx/dpi    // width in dp

export default
{
    dpi,
    hpx,
    wpx,
    hdp,
    wdp,
};
